// Módulo (%)
// Operardor binário - Retorna o inteiro restante da divisão dos dois operandos.

12 % 5 // retorna 2.

// Incremento (++) e Decremento (--)
++X
X++

let a = 7;
const b = a++; // b recebe 7 e incrementa a
const c = ++a; // incrementa a e atribui a c
console.log(a, b, c);

--X
X--

// Negação (-) e Adição (+)
-3
+"3"    //Retorna 3
+true   //Retorna 1
+false  //Retorna 0
-true   //Retorna -1


// Operador de exponenciação (**)
2 ** 3   //Retorna 8
10 ** -1 //Retorna 0.1

//operador de agrupamento ()
2 * (3 +2)